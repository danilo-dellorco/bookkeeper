DOMAIN PARTITIONING

BLACK BOX
ledgerId: 	intero che in quanto ID (semantica nome) mi aspetto debba essere maggiore di zero
	--> {-1,0,1} mi aspetto che -1,0 diano errore

entryId:	intero che in quanto ID (semantica nome) mi aspetto debba essere maggiore di zero
	--> {-1,0,1} mi aspetto che -1,0 diano errore

entry:		tipo di dato complesso quindi considero una istanza valida una non valida e una nulla
	--> {valid_entry,invalid_entry,null}